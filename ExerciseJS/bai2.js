function Array(string) {
    var sum = 0;
    for(i = 0; i < string.length; i++) {
        // if(string === '*') {
        //     // sum = sum + (+[string]);
        //     console.log(+(string));
        // }
    }
}

console.log(Array('1*4'));