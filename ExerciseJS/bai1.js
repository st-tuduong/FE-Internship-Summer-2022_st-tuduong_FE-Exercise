function Sum(a,b) {

    // option1
    // if(a>b){
    //    return (a + b);
    // }
    // else{
    //     return ((a + b) * 3);
    // }

    // option2 Recommend
    if(a>b){
       return (a + b);
    }
        return ((a + b) * 3);
    }

console.log(Sum(6,4));